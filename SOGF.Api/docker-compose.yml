services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      SA_PASSWORD: "Senha123"
      ACCEPT_EULA: "Y"
      MSSQL_PID: "sa"
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/mssql
volumes:
  sql_data:
    